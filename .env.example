# comma-separated "user:pass" pairs — the scraper only uses one account at a
# time and fails over to the next only if the current one's login fails
LINKEDIN_ACCOUNTS=first_dummy@example.com:first_password,second_dummy@example.com:second_password
REQUEST_TIMEOUT_MS=30000
RATE_LIMIT_PER_MINUTE=5
